otc: "0.2.0"
id: "okham.event.id"
name: "Event Identifier"
description: "Identifier for an event instance."
version: "1.0.0"
status: "stable"
fields:
  - name: "eventId"
    type: "string"
    required: true
    description: "Unique identifier for the emitted event instance."

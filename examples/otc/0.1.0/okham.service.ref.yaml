otc: "0.2.0"
id: "okham.service.ref"
name: "Service Reference"
description: "Reference to a service or capability identity."
version: "1.0.0"
status: "stable"
fields:
  - name: "serviceId"
    type: "string"
    required: true
    description: "Identifier of the service or capability."
  - name: "version"
    type: "string"
    required: false
    description: "Version of the referenced service."

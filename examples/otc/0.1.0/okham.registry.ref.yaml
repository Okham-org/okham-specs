otc: "0.2.0"
id: "okham.registry.ref"
name: "Registry Reference"
description: "Reference to a registry entry in a catalog."
version: "1.0.0"
status: "stable"
fields:
  - name: "registryId"
    type: "string"
    required: true
    description: "Identifier of the registry or catalog."
  - name: "entryId"
    type: "string"
    required: true
    description: "Identifier of the entry in the registry."

otc: "0.2.0"
id: "okham.slug"
name: "Slug"
description: "URL-safe slug identifier."
version: "1.0.0"
status: "stable"
fields:
  - name: "value"
    type: "string"
    required: true
    description: "Slug value suitable for URLs and human-readable IDs."

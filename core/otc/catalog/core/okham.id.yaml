otc: "0.2.0"
id: "okham.id"
name: "Identifier"
description: "Canonical identifier value for entities and records."
version: "1.0.0"
status: "stable"
fields:
  - name: "value"
    type: "string"
    required: true
    description: "Identifier value in its canonical string form."
  - name: "namespace"
    type: "string"
    required: false
    description: "Optional namespace that scopes the identifier."

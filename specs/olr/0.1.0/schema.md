# OLR Schema

Canonical schema: `https://okham.org/schemas/olr/0.1.0/schema.json`

This schema is intentionally minimal for v0.1.0:

- validates the *shape* of a ruleset
- leaves `assert` semantics and `params` contents tool-defined

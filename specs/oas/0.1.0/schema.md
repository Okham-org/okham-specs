# OAS Schema

Canonical schema: `https://okham.org/schemas/oas/0.1.0/schema.json`

This schema focuses on:

- stable identifiers (`assertionId`, `version`)
- a normative `description`
- a `paramsSchema` JSON Schema (draft 2020-12) describing OLR `rule.params`

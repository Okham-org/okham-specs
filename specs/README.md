# specs/ (Normative)

This directory contains the **canonical specification documents** for OKHAM contracts.

These documents are **normative** (requirements, MUST/SHOULD language, versioning rules).

If you’re looking for machine-readable validation, see `schemas/`.
